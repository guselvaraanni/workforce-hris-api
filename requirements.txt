# ─── Core Django ─────────────────────────────────────────────────────────────
Django==4.2.13
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
django-filter==24.2               # Proper field-level API filtering
drf-spectacular==0.27.2           # OpenAPI 3 schema + Swagger UI

# ─── MySQL ───────────────────────────────────────────────────────────────────
mysqlclient==2.2.4                # Django MySQL adapter (native C binding, fastest)
# Alternative if mysqlclient fails to compile: PyMySQL==1.1.1

# ─── Environment & Config ────────────────────────────────────────────────────
python-dotenv==1.0.1

# ─── Debug & Dev ─────────────────────────────────────────────────────────────
django-debug-toolbar==4.3.0       # Query count / timing in browser

# ─── Testing ─────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-django==4.8.0
factory-boy==3.3.0
Faker==25.2.0
pytest-cov==5.0.0                 # Coverage reports
pytest-xdist==3.5.0              # Parallel test execution: pytest -n auto

# ─── Celery (future — background tasks) ──────────────────────────────────────
# Uncomment when ready to switch from threading to Celery:
# celery==5.4.0
# redis==5.0.7
# django-celery-results==2.5.1
# flower==2.0.1                   # Celery monitoring UI

# ─── Caching ─────────────────────────────────────────────────────────────────
# django-redis==5.4.0             # Uncomment when Redis is available

# ─── Production server ───────────────────────────────────────────────────────
gunicorn==22.0.0
